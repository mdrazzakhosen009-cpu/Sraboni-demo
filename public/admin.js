let pass='',products=[],orders=[],reviews=[];const $=s=>document.querySelector(s);async function api(u,o={}){o.headers={...(o.headers||{}),'x-admin-password':pass};let r=await fetch(u,o);return r.json()}$('#go').onclick=async()=>{pass=$('#pw').value;let r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:pass})}).then(x=>x.json());if(r.ok){$('#login').style.display='none';$('#app').style.display='flex';load()}else $('#msg').textContent='Wrong password'};document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tab').forEach(x=>x.style.display='none');$('#'+b.dataset.tab).style.display='block';$('#title').textContent=b.textContent;load()});$('#logout').onclick=()=>location.reload();async function load(){products=await api('/api/products');reviews=await api('/api/reviews');orders=await api('/api/admin/orders');$('#sp').textContent=products.length;$('#so').textContent=orders.length;$('#sr').textContent=reviews.length;$('#pt').innerHTML='<tr><th>Name</th><th>Category</th><th>Price</th><th>Stock</th></tr>'+products.map(p=>`<tr><td>${p.name}</td><td>${p.category}</td><td>৳${p.price}</td><td>${p.stock}</td></tr>`).join('');$('#ot').innerHTML='<tr><th>Order</th><th>Customer</th><th>Total</th><th>Status</th></tr>'+orders.map(o=>`<tr><td>${o.orderNo}</td><td>${o.customer?.name||''}</td><td>৳${o.total}</td><td><select onchange="status('${o.orderNo}',this.value)"><option>${o.status}</option><option>Processing</option><option>Shipped</option><option>Delivered</option><option>Cancelled</option></select></td></tr>`).join('');$('#rv').innerHTML=reviews.map(r=>`<div class="panel"><b>${r.name}</b> — ★★★★★<p>${r.text}</p></div>`).join('');let s=await fetch('/api/settings').then(x=>x.json());['wa','fb','ig','tt','yt'].forEach((x,i)=>$('#'+x).value=[s.whatsapp,s.facebook,s.instagram,s.tiktok,s.youtube][i]||'')}async function status(no,s){await api('/api/admin/orders/'+no,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:s})});load()}$('#save').onclick=async()=>{await api('/api/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({whatsapp:$('#wa').value,facebook:$('#fb').value,instagram:$('#ig').value,tiktok:$('#tt').value,youtube:$('#yt').value})});alert('Saved')};
